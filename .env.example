# waza Web UI Environment Configuration
# Copy to .env and fill in values

# GitHub OAuth (optional - enables authenticated features)
# Create at: https://github.com/settings/applications/new
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Custom data directory (default: ~/.waza)
# WAZA_DATA_DIR=/path/to/data
